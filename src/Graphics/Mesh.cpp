#include "Mesh.h"

Mesh::Mesh(float* vertices, uint32_t* indices, uint32_t verticeCount, uint32_t indiceCount) : vertices(vertices), indices(indices), verticeCount(verticeCount), indiceCount(indiceCount)
{
	
}